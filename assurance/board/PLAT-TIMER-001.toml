# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 RS-Key contributors
#
# Every timeout claim in the model rests on this: the presence window, the
# pinUvAuthToken's rolling and absolute caps, and the reset window CTAP 2.1
# §6.6 bounds. A clock that stops makes each of them vacuously safe.

assumption = "PLAT-TIMER-001"
method = "Read the RP2350 timer's clock source against the boot configuration the image actually sets, then run the board long enough to cross the counter's wrap and observe that a timeout armed before the wrap still expires after it."
boot_config = "The DEFAULT image, because the clock source is set by this tree's own boot code and a different build could set a different one."
expected = "`embassy_time::Instant` is monotonic across the wrap and every armed timeout expires. PASS = expiry observed after the wrap. FAIL = a timeout that never fires, which turns the presence window into no window at all."
outcome = "planned"
board = ""
stepping = ""
firmware_sha256 = ""
first_boot_capture = ""
actual = ""
