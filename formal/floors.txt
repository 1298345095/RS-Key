\* SPDX-License-Identifier: AGPL-3.0-only
\* Copyright (C) 2026 RS-Key contributors
\*
\* WHAT EACH CONFIGURATION MUST PRODUCE, so that a run nobody watched cannot
\* pass by getting SMALLER. `Seams.cfg` once came back GREEN over ONE distinct
\* state -- every invariant holding vacuously, because a precedence slip had
\* turned an assignment into a guard that disabled both SELECT actions (E164).
\* A one-state GREEN is indistinguishable from a real one in the log, and the
\* runner's `VACUOUS` heuristic only sees the collapse all the way to nothing.
\*
\* The floor is an ORDER OF MAGNITUDE, not a pin. It is set near a third of the
\* measured count so that ordinary fidelity churn does not trip it -- this model
\* has legitimately shrunk 23% in one round -- while a collapse cannot pass. A
\* GREEN below its floor is reported as FLOOR and exits non-zero; re-measure and
\* move the number deliberately, the way the gate's other ratchets move.
\*
\* The VERDICT column is the second half, and it catches the other silent pass:
\* a mutant that stops firing. `BugSetPinKeepsPpuat` explored 40 459 667 states
\* without a counterexample after a fix made its defect unreachable (E171) --
\* far above any floor, and only visible because somebody read the matrix.
\*
\* And a RED verdict alone is not the third half. A mutant can go red for a
\* defect it does not model -- 2 of 24 co-refutation patches in this tree scored
\* a kill that way -- so a RED row may name the invariant it must break, and the
\* runner compares it. Measured on this file: flipping the alwaysUv mutant to the
\* INVERSE defect kept `TraceSecurityBadAlwaysUvArm.cfg` red, on a different
\* boundary and a different direction, and nothing noticed until the name was
\* compared. The `Mut_*`/`Solo_*` families already name theirs through
\* `gen-configs.sh`'s INVARIANTS block, so the column is for the rows that do not.
\*
\* The depths in the comments below are the WORKERS=1 reading, and they are the
\* one number here that nothing compares. TLC reports the deepest BFS level its
\* workers OPENED, which overshoots the diameter by one when a worker starts the
\* next level early -- `Store.cfg` is 6 at one worker over six runs, 6 or 7 at the
\* default two over thirteen, 7 at four and at eight. A ratchet on it would be red
\* or green by scheduler, so there is none: re-measure by hand with
\* `WORKERS=1 ./run-tlc.sh <cfg>`.
\*
\* Columns: <config or glob>  <GREEN|RED>  <min distinct, or - >  [heap]  [invariant]
\* First match wins, so an exact name may precede its class glob.

Shipped.cfg                          GREEN   20000000
\* AS-AUTH-2's other arm: the same six invariants over a build that SHIPS
\* alwaysUv. 23 521 512 distinct at depth 51 on a reduced retry ladder (2 : 1) --
\* the arm buys the reachability question, not the ladder -- so a third is the
\* floor, the same rule as its siblings.
AlwaysUv.cfg                         GREEN   7800000
\* `WidePerms`'s other arm: all sixteen permission subsets a host can obtain,
\* against the five the model carries. One relying party, because the wide
\* domain is what is being paid for, and two channels because scopes.txt holds
\* that minimum for NoAuthorizationBypass, which this configuration checks --
\* the counts are the results table's, not this comment's. A third of the
\* measured distinct count is the floor, the same rule as its siblings; the
\* narrow arm of the same scope was measured at 9 472 044 in 142 s and is not a
\* recorded row, so it is here rather than there.
PermWide.cfg                         GREEN   7000000
\* `ForceChangeModelled`'s other arm: EF_MINPINLEN[1], the forced-PIN-change
\* flag, which the module did not carry at all until PLAT-MODEL-010 measured the
\* gap. PermWide.cfg's constants for the same reason -- the question is the gate,
\* not the ladder -- and alwaysUv ON because with no PIN there is nothing to
\* pend. A third of the measured 11 977 584 distinct states is the floor, the
\* same rule as its siblings.
ForceChange.cfg                      GREEN   4000000
Seams.cfg                            GREEN   200
\* The flash layer (RSKeyStore): a small exhaustive space -- 364 distinct at
\* depth 6 -- so its floor is set near a third, the same rule as its siblings.
Store.cfg                            GREEN   90
\* The retry/recovery lattice (RSKeyRetryLattice): 243 distinct at depth 11, five
\* budgeted references over a ceiling of two -- a third is the floor here too.
Lattice.cfg                          GREEN   80
\* Four-app operation policies (RSKeyAppletPolicies): 2268 distinct at depth 14;
\* one third is the floor, separate from the real PIV/OpenPGP retry counters.
Policies.cfg                         GREEN   750
\* The administrative surface (RSKeyAdminSurface): 8 distinct -- a 3-capability
\* mask whose properties are step ghosts, so the space IS the mask's power set.
\* A third rounds to 3; below it the mask itself has collapsed.
Admin.cfg                            GREEN   3
\* The trusted-display ceremony (RSKeyTrustedDisplay): 5 distinct. At this size
\* the third-rule stops serving -- losing even one state means an action died --
\* so the floor sits one under the measurement instead.
Display.cfg                          GREEN   4
\* The cross-boot hardening (RSKeyBootHardening): 24 distinct at depth 5, the
\* marker/weak/lock product across the reset line -- a third, as usual.
Boot.cfg                             GREEN   8
\* The same module with the open hardware assumption taking its other arm: a
\* power-on that does NOT clear the scratch word makes a cold reset a warm one,
\* so 6 of the 24 distinct states become unreachable. Both invariants still hold
\* and all three boot mutants still redden on the same invariant -- the
\* assumption buys REACHABILITY, not safety, which is what running it says.
BootCarry.cfg                        GREEN   6
\* The CTAPHID reassembler (RSKeyTransport): 13 distinct at depth 4, the
\* owner/seq/got/need product over two channels -- a third rounds to 4.
Transport.cfg                        GREEN   4
\* Phase 4, trace validation: a replay is LINEAR (distinct = trace length + 1),
\* and the length changes whenever the session is re-recorded -- so the floor
\* is low and the real guarantee is the verdict pair: a divergence in the
\* recorded session deadlocks (GREEN row goes RED), and the Bad row is the
\* harness proven able to refuse a session at all.
TraceSeams.cfg                       GREEN   5
TraceSeamsBad.cfg                    RED     -
\* Full RSKeySecurityState refinement replay: 49 inferred B micro-steps from
\* 21 real CBOR boundaries across three suites and one replug. The α mutant's
\* GREEN control, with R4b disabled, proves that the corresponding RED row is
\* caught by R4b alone. The last two are R4c's, one per half of the gate rule,
\* so a RED names which half was load-bearing rather than "a gate somewhere".
\* The two GREEN rows are PINS, not thirds: a linear replay of 74 forced steps
\* has exactly 75 states, so anything less is the model refusing to follow the
\* recording. It reached 44 of 59 for three days and read as GREEN.
TraceSecurity.cfg                    GREEN   75
TraceSecurityBadBeta.cfg             RED     -       -       R4aRawRefinesB
TraceSecurityBadAlpha.cfg            RED     -       -       R4bAlphaMatchesGamma
TraceSecurityBadAlphaNoR4b.cfg       GREEN   75
TraceSecurityBadOutcome.cfg          RED     -       -       R4bEventConsensus
TraceSecurityBadUvNotRqd.cfg         RED     -       -       R4cGateAnswers
TraceSecurityBadResetWindow.cfg      RED     -       -       R4cGateAnswers
TraceSecurityBadAlwaysUvArm.cfg      RED     -       -       R4cGateAnswers
TraceSecurityBadPinSet.cfg           RED     -       -       R4cGateAnswers
\* The recorded session's coverage, ratcheted so a re-record cannot quietly cover
\* less. Unlike the distinct-state floors above these are PINS at the measured
\* value, not a third of it: a trace is a fixed recording, so it cannot shrink by
\* fidelity churn -- only by someone recording a shorter session. Raise them when
\* a richer session lands; never lower one without saying which flow was dropped.
@TraceSecurityCommandsMin 40
\* 58 until R4c, and every one of the nine that went was a MIS-MAPPING rather
\* than coverage. Seven: the refused reset that ends `27_reset_window` was
\* replayed as a full successful wipe, because over an already-empty store the
\* two have the same raw footprint — it is one `Tick` and a stutter now, and the
\* window decides. Two more: the sweep emitted one step per live record, but the
\* seed arm empties `cred` and `rpent` with it, so the phase is two steps however
\* many credentials B holds. The model had been refusing the surplus since the
\* three-suite recording landed, and TLC was run with `-deadlock`.
@TraceSecurityStepsMin 74
@TraceSecurityActionsMin 22
\* Boundaries whose outcome B answers from a GATE rule rather than from the
\* actions -- there are none to read, both sides of each gate being a stutter.
\* Without a floor here R4c is vacuous the moment a re-record loses them. Two of
\* the seven carry `pin.set` FALSE (`tests/09_tokenless_gate_no_pin.py`), and
\* they are the only ones `TraceSecurityBadPinSet.cfg` can go red at: against the
\* recording without them that mutant replays green to the end.
@TraceSecurityGatesMin 7
\* Was 2, and both were the same shape: a token-less makeCredential, whose
\* success and refusal write nothing either way. R4c answers them from `rk` and
\* B's own state, so the replay commits to an outcome at every boundary it maps.
@TraceSecurityAmbiguousMax 0
\* Boundaries where B COMMITS to an outcome and R4b compares it with the device's.
\* 12 until the recording carried the clientPIN SUBCOMMAND: a token re-issued with
\* the permissions it already holds moves no raw field, so it arrived as a bare
\* stutter and was indistinguishable from the `getKeyAgreement` that shares that
\* footprint. Measured both ways -- with the rule 13, with `TOKEN_SUBCOMMANDS`
\* emptied 12 -- because a mapping that retreats to NO-OPINION costs nothing and
\* reads exactly as green without a floor here.
@TraceSecurityOutcomesMin 13
\* Tier A's requirement half (RSKeyTokenGate). The first floor is a PIN at 44
\* and not a third: NoAuthorizationBypassA is asserted over the relation's SLICE
\* at the current state, so it covers AllowedRelation only if every A state is
\* reachable -- and 44 is the whole domain. Lose one and the invariant quietly
\* stops being about the edges out of it.
TokenGate.cfg                        GREEN   44
\* The oracle's non-degeneracy, run as a probe rather than a behaviour: every
\* state on which the requirement and the relation disagree is an INITIAL state,
\* so the distinct count IS the size of that set and 22 is a PIN too. An oracle
\* transcribed from the relation empties it, and TLC refuses an empty INIT.
TokenGateOracle.cfg                  GREEN   22
\* E4a. The two are not one predicate, and this row is what says so; a GREEN
\* here is the refuted per-FID projection repeating itself one tier up.
TokenGateDisagreement.cfg            RED     -       -       RequiredGateAgreesWithRelation
\* One Authorized edge the requirement forbids, so the invariant above is known
\* able to fail rather than assumed to be.
TokenGateMut_BugUnauthorizedEdge.cfg RED     -       -       NoAuthorizationBypassA
\* Phase 5. Reduced constants keep the labelled action check separate from the
\* 60-million-state shipped safety row. Floors are calibrated after the first
\* successful run; the non-vacuity guard still requires at least two states.
TokenRefinement.cfg                  GREEN   2500000
TokenRefinementBadMap.cfg            RED     -
TokenRefinementOutcome.cfg           GREEN   2500000
TokenRefinementDeadToken.cfg         RED     -
\* The liveness layer builds a behaviour graph ON TOP of the state graph -- 23.7
\* million nodes at these constants -- and runs out of memory at the 4 GB
\* default after the state search has already completed (E172). The heap column
\* is that one-line fix, kept beside the number that motivates it.
Liveness.cfg                         GREEN   2000000   12g
Liveness_Full.cfg                    GREEN   2000000   24g
\* The fairness-shape check: the same reduced constants, `Spec` rather than
\* `FairSpec`, and eighteen ENABLED evaluations per state for about 5% more wall
\* clock than the plain safety run.
Fairness.cfg                         GREEN   2000000

\* Every mutant must FIRE. A counterexample search halts at the first violation,
\* so its state count is worker-scheduling dependent and carries no floor.
\*
\* The reason is compared too, and where these rows say nothing the runner derives
\* it -- see `run-tlc.sh`. Registering an exception here for the one configuration
\* the derivation cannot predict was tried and REFUSED: an exact row in front of
\* its class glob is exactly what `scripts/verdict_gate.py` rejects, because
\* nothing in this tree can tell first-match from last-match. The header above
\* advertises that mechanism; the gate forbids it, and the gate wins.
Historical_*.cfg                     RED     -
Mut_*.cfg                            RED     -
Solo_*.cfg                           RED     -
SoloClause_*.cfg                     RED     -
SeamMut_*.cfg                        RED     -
SeamSolo_*.cfg                       RED     -
StoreMut_*.cfg                       RED     -
StoreSolo_*.cfg                      RED     -
\* The store's induction probe, and it is the one that FOUND something.
\* `StoreInv` alone is not inductive: from a type-correct state where the cache
\* says EF_META is absent while a record stands, `MetaAdd` trusts it and rebuilds
\* the blob from empty. `CacheHonest` is the conjunct the counterexample named.
\* 1000 of the module's type-correct states survive `IndInv`; the floor is that
\* count, because below it the predicate stopped admitting what it admitted.
StoreInduction.cfg                   GREEN   1000
\* One mutant, for the reason the boot family's row gives.
StoreInductionMut_*.cfg              RED     -
LatMut_*.cfg                         RED     -
LatSolo_*.cfg                        RED     -
PolicyMut_*.cfg                      RED     -
PolicySolo_*.cfg                     RED     -
AdminMut_*.cfg                       RED     -
AdminSolo_*.cfg                      RED     -
DispMut_*.cfg                        RED     -
DispSolo_*.cfg                       RED     -
BootMut_*.cfg                        RED     -
BootSolo_*.cfg                       RED     -
\* The same three on the assumption's FALSE arm, ONE invariant each: a power-on
\* that leaves the scratch word standing changes what is REACHABLE (18 distinct
\* against 24), and these rows are what say it changes no verdict. Solo-style so
\* a RED here names the same defect the TRUE arm's row names -- a full-invariant
\* configuration would let a sibling report a mutant gone unreachable.
BootCarryMut_*.cfg                   RED     -
\* The induction probe: `INIT IndInv` / `NEXT Next`, so every type-correct state
\* the invariants admit is an initial state. DEPTH 1 is the claim -- every
\* successor already being initial IS `IndInv /\ Next => IndInv'` -- and the
\* runner holds these rows to it instead of to the vacuity depth floor, which
\* would refuse them all. 48 of the module's 108 type-correct states survive
\* `IndInv`; the floor is that count. Both probes pin the scratch word CLEARED:
\* what they vary is the state, not the hardware assumption.
BootInduction.cfg                    GREEN   48
\* ONE mutant of it, and only one. `Init` satisfies `IndInv`, so a defect that
\* breaks the step from an admitted state also breaks it from a reachable one --
\* the induction mutants fire on a SUPERSET of their `*Solo_` twins' conditions
\* and so cannot see a mutant that stopped firing, which is what the verdict
\* column exists for. This row says the INIT/NEXT wiring can go red; the Solo
\* rows are what carry the defects.
BootInductionMut_*.cfg               RED     -
TransMut_*.cfg                       RED     -
TransSolo_*.cfg                      RED     -
LiveMut_*.cfg                        RED     -
FairMut_*.cfg                        RED     -
